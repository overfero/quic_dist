__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/

# Compiled Rust extension - built locally via `cd rust && cargo build
# --release`, not committed as a binary blob (see README.md).
_rust_quic_engine*.so

# Rust build cache - regenerated by `cargo build`, can be tens of GB.
rust/target/
